## Python Telegram Bot

A template for incoming projects and tasks.
To implement just clone the repo using the below commands:


$ git clone https://github.com/Horadmard/python_telegram_bot.git

$ cd python_telegram_bot

Enter your bot token and run the PTB.py script on a client with access to the unfiltered internet.

$ touch TOKEN >> config.json
