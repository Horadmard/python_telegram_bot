## Python Telegram Bot

A template for incoming project and tasks.
To impliment just clone the repo using below commands:


$ git clone https://github.com/Horadmard/python_telegram_bot.git

$ cd python_telegram_bot

Enter your bot-token and run the PTB.py script on a client that access to unfiltered internet.

$ touch TOKEN >> config.json
